# Render.com Environment Configuration
# Copy this to .env.render and update with your values

# Database Configuration (PostgreSQL) - Render will provide these
DB_HOST=${DB_HOST}
DB_PORT=${DB_PORT}
DB_USERNAME=${DB_USERNAME}
DB_PASSWORD=${DB_PASSWORD}
DB_NAME=${DB_NAME}
DB_SYNC=false
DB_SSL=true

# Cookie Configuration - Generate a secure key
COOKIE_KEY=your-secure-cookie-key-here

# Application Configuration
NODE_ENV=production
PORT=10000
ALLOWED_ORIGINS=https://your-frontend-domain.onrender.com

# Test Configuration (Optional)
TEST_PASSWORD=testpassword123
